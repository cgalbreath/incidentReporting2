<%= nested_form_for(@program, remote: true, :html => { :role => "form" }) do |f| %>
  <div id="error_explanation" class="bg-danger text-danger">

  </div>

  <div class="row">
    <div class="col-sm-6">
      <div class="form-group">
        <%= f.label :name, :class => "control-label" %>
        <%= f.text_field :name, :class => "form-control" %>
      </div>  
    </div>
  </div>

  <div class="row">
    <div class="text-center">
      <div class="form-group">
        <%= f.submit  @program.new_record? ? "Create Program" : "Update Program", :class => "btn btn-primary" %>
      </div>
    </div>
  </div>
<% end %>