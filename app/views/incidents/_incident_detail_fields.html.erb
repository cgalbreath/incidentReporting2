<div class="panel panel-default">
	<div class="panel-heading">
		TEST
		<button type="button" class="close" data-dismiss="modal" aria-label="Close">
			<span aria-hidden="true">×</span>
		</button>	
	</div>
	<div class="panel-body">
		<div class="row">
			<div class="col-sm-4">
			    <div class="form-group">
					<%= f.label :incident_type_id, :class => "control-label" %>
					<%= f.select :incident_type_id, IncidentType.all.collect {|p| [ p.name, p.id ] }, { include_blank: true }, {:class => 'form-control'} %>
				</div>
			</div>
			<div class="col-sm-4">
				<div class="form-group">
					<%= f.link_to_remove "Remove detail" %>
				</div>
			</div>
		</div>
		<%= render 'sub_incident', f: f %> 
	</div>
</div>